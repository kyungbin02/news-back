# 경빈 S3 추가 - Nginx 파일 크기 제한 증가
files:
  "/etc/nginx/conf.d/01_nginx.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      client_max_body_size 50M;

container_commands:
  01_reload_nginx:
    command: "service nginx reload || true"

