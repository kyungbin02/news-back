# dev 프로필 전용 설정 (로컬 개발용)
spring:
  config:
    activate:
      on-profile: dev
  
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/myapp?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: myapp
    password: 1111
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
  security:
    oauth2:
      client:
        registration:
          naver:
            client-id: Mvjok67aLB8GDDaZn0Hk
            client-secret: 0mwExilKi6
            redirect-uri: http://localhost:8080/login/oauth2/code/naver
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            scope:
              - name
              - email
              - profile_image
          kakao:
            client-id: cca642db7300e1c808f9aacaf9894c0a
            client-secret: LJsMaH2dXOamJJ48Y66M03TA036jFe7D
            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            scope:
              - profile_nickname
          google:
            client-id: 1007418449410-sakp785ndehik3122ni9meufn3r5kten.apps.googleusercontent.com
            client-secret: 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
            redirect-uri: http://localhost:8080/login/oauth2/code/google
            authorization-grant-type: authorization_code
            scope:
              - profile
              - email
        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
            user-name-attribute: sub
  
  servlet:
    multipart:
      max-file-size: 1MB       # 개별 파일 최대 크기 (기본값)
      max-request-size: 10MB   # 전체 요청 최대 크기 (기본값)
      enabled: true
      file-size-threshold: 2KB

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: myapp.backend.domain
  configuration:
    map-underscore-to-camel-case: false

server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

jwt:
  secret: myapp-jwt-secret-key-2024-very-long-and-secure-key-for-jwt-token-generation
  expiration: 86400000  # 24시간 (밀리초)

# 프론트엔드 URL 설정 (로컬 개발용)
frontend:
  url: http://localhost:3000

gemini:
  api:
    key: AIzaSyC9nv3TxsxA1Xy-QwLxxwDYG-4s9TbRUYE  # 직접 설정

# 경빈 S3 추가 - 공통 AWS/S3 설정
cloud:
  aws:
    credentials:
      access-key: ${AWS_ACCESS_KEY:}
      secret-key: ${AWS_SECRET_KEY:}
    region: ${AWS_REGION:ap-northeast-2}
    s3:
      bucket: ${S3_BUCKET_NAME:newsgo-images}
      folder: columns
